export { api, setAuthToken, clearAuthToken } from "./client";

// Domain clients
export * from "./domains";

// React Query hooks
export * from "./hooks";

// Factories (for creating custom hooks)
export { createQuery, createMutation } from "./factories";